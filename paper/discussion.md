\hl{The novelty of the proposed Modality Decoupling and Coupling Network (MDCN) lies not merely in the incremental combination of vision-language models and feature separation, but in a fundamental paradigm shift regarding how identity and modality spaces interact. Unlike existing visible-infrared person re-identification (VI-ReID) frameworks that suffer from representation degradation, MDCN introduces three core conceptual and architectural breakthroughs:}

\hl{
Traditional learnable text prompts inevitably fall into a dilemma of passive entanglement, where modality prompts accidentally absorb identity cues and person prompts are forced to accommodate massive cross-modal visual discrepancies. CPDL breaks this limitation by pioneering an explicit semantic factorization mechanism. By decomposing a holistic modality-contextual prompt into independent, structurally isolated person-related and modality-related components, and enforcing a strict modality orthogonality constraint ($\mathcal{L}_{mo}$), we create a zero-redundancy boundary between identity definitions and sensor specificities. This shifts prompt learning from traditional empirical tuning to a mathematically sound, disentangled textual anchor system.}

\hl{
Existing feature-level alignment techniques usually rely on brute-force image-text matching or feature concatenation, which renders the model highly susceptible to real-world visual degradations such as low-light noise, background clutter, and severe illumination variations. SCL redefines cross-modal alignment by transforming these decoupled text anchors into frozen, high-level semantic class prototypes. Because natural language represents absolute, high-level abstract concepts immune to pixel-level noise, forcing a three-branch visual encoder to geometrically converge toward these stable textual prototypes establishes a domain-invariant coordinate system in the multimodal latent space. This conceptual leap regularizes chaotic visual feature distributions into structured, noise-resistant semantic clusters rather than passively matching local visual patterns.}

\hl{
While standard knowledge distillation methods focus on duplicating vanilla feature representations, they fail to inherit the underlying geometric topology of heterogeneous feature spaces. MRKD addresses this by introducing a hierarchical cosine angular distillation paradigm that operates on a philosophy of selective structural forgetting. By deliberately discarding cross-modal relations in the specific branch and intra-modal details in the shared branch, we proactively prevent cross-space semantic contamination. This tactical erasure forces the holistic modality-contextual space ($f_{co}$) to inherit only the pure relational essence—simultaneously gaining intra-modal compactness and cross-modal consistency. MDCN thus pioneers a structurally complementary, relationally enriched representation framework that eliminates semantic confusion during multi-space information fusion.}

\hl{In summary, through the synergistic interplay of CPDL, SCL, and MRKD, MDCN establishes a semantically clean, structurally complementary, and relationally enriched cross-modal representation learning framework that significantly elevates cross-spectral identity retrieval.}






Although MDCN achieves strong cross-modal retrieval performance by decoupling modality-contextual prompts, guiding multi-branch visual feature learning, and distilling relational knowledge into the modality-contextual space, severe structural partial occlusion remains an important limitation. In real surveillance scenarios, persistent occluders such as vehicles, pillars, or crowds may not only remove discriminative body regions, but also introduce misleading contextual cues into the visual-textual alignment process. Since the learnable prompts in CPDL are optimized through image-text contrastive constraints, long-range missingness may cause the prompts to absorb semantic information from occluding objects rather than from the target identity, thereby weakening the semantic purity of person-related and modality-related prompts and perturbing the hierarchical angular relations used in MRKD. Moreover, severe occlusion can lead to spatial feature misalignment across non-overlapping visible and infrared views, especially when key identity-related parts visible in one modality are completely absent in the other. To address this issue, future work could extend MDCN with part-level structural reasoning by incorporating a part-aware feature extraction into the multi-branch visual network to suppress occlusion-induced noise, and by adopting local part-to-part matching during inference to alleviate cross-view and cross-spectral mismatches. We believe that such part-based extensions would further enhance the robustness of MDCN under complex real-world deployment conditions.

Although MDCN achieves strong cross-modal retrieval performance by decoupling modality-contextual prompts, guiding multi-branch visual feature learning, and distilling relational knowledge into the modality-contextual space, severe partial occlusion remains a limitation. In real surveillance scenes, objects such as vehicles, pillars, or crowds may cover important body parts and bring misleading background information into the visual-textual alignment process. Since the learnable prompts in CPDL are optimized by image-text contrastive learning, long-term missing body regions may make the prompts capture information from occluding objects instead of the target person. This can weaken the person-related and modality-related prompts and affect the hierarchical angular relations used in MRKD. In addition, severe occlusion may cause spatial misalignment between visible and infrared images, especially when key identity cues appear in one modality but are missing in the other. To reduce these effects, future work could add part-aware feature extraction to the multi-branch visual network, so that the model can focus more on visible and reliable body regions. Local part-to-part matching can also be used during inference to reduce cross-view and cross-spectral mismatches. These extensions may further improve the robustness of MDCN in complex real-world scenarios.


\hl{Although the proposed MDCN achieves strong cross-modal retrieval performance under standard real-world degradations, it is worth discussing its bound behavior under severe, structural partial occlusion notorious challenge in surveillance analytics. Partial occlusion introduces two deep-seated spatial difficulties for VI-ReID. First, when a pedestrian is significantly blocked by persistent physical obstacles (e.g., vehicles or pillars), these long-range occlusions do not merely introduce pixel-level noise, but induce acute semantic drifts during the visual-textual alignment phase. Since the continuous text tokens in Stage I are optimized via image-text contrastive constraints, prolonged structural missingness forces the learnable prompts to inadvertently assimilate the semantic attributes of the occluding entities rather than the actual identities. This localized contextual distortion ultimately perturbs the geometric topology of the hierarchical angular relations in MRKD. Second, severe occlusion fundamentally triggers severe spatial feature misalignment across non-overlapping views, where key discriminative parts present in the query modality are entirely absent in the gallery spectrum.}

\hl{To mitigate these adverse effects, two future dimensions appear promising. First, a part-level feature extraction mechanism could be integrated into the multi-branch visual network to explicitly focus on visible, unobstructed body regions, thereby filtering out occlusion-induced noise at the pixel level. Second, a local matching strategy can be adopted during the inference phase, where fine-grained part-to-part correspondences are established to explicitly alleviate the cross-view cross-spectral feature mismatch problem. We leave these extensions for future exploration and believe that augmenting the MDCN paradigm with part-based structural reasoning will further solidify its robustness under complex, real-world deployment conditions.}



>Although MDCN achieves strong cross-modal retrieval performance by decoupling modality-contextual prompts, guiding multi-branch visual feature learning, and distilling relational knowledge into the modality-contextual space, severe partial occlusion remains a limitation. In real surveillance scenes, objects such as vehicles, pillars, or crowds may cover important body parts and introduce misleading background cues into the visual-textual alignment process. Since the learnable prompts in CPDL are optimized through image-text contrastive learning, missing body regions may cause the prompts to capture information from occluding objects rather than from the target person, which can weaken semantic guidance and affect the hierarchical angular relations used in MRKD. To reduce these effects, future work could extend MDCN by incorporating part-aware feature extraction into the multi-branch visual network, allowing the model to focus more on visible and reliable body regions. Human-in-the-loop feedback could also be introduced to help the model identify unreliable occluded regions, correct misleading retrieval results, and guide prompt learning with human-provided supervision. These extensions may further improve the robustness of MDCN in complex real-world scenarios.




本文的创新点主要体现在：提出了一种面向可见光-红外行人重识别的模态解耦与耦合网络（MDCN），通过引入模态上下文文本提示，将身份相关语义与模态相关语义进行显式解耦，从而缓解传统文本提示中身份信息与模态差异混杂的问题；同时，设计语义耦合学习机制，利用解耦后的高层语义提示分别引导模态共享特征、模态特定特征和模态上下文特征的学习，使模型在保持身份判别信息的同时更好地建模跨模态一致性与模态差异性；此外，进一步提出模态关系知识蒸馏方法，通过层次化余弦角度蒸馏将模态特定空间中的类内关系和模态共享空间中的跨模态关系融合到模态上下文特征空间中，从而有效降低跨模态差异与模态内干扰，提升特征表达的判别性和鲁棒性。


The main innovation of this work lies in proposing a Modality Decoupling and Coupling Network (MDCN) for visible-infrared person re-identification. MDCN introduces modality-contextual text prompts and explicitly decouples them into identity-related and modality-related semantic prompts, thereby alleviating the entanglement between identity information and modality discrepancies in existing prompt-based methods. Furthermore, a semantic coupling learning strategy is designed to use the decoupled high-level semantic prompts to guide the learning of modality-shared, modality-specific, and modality-contextual visual features, enabling the model to preserve discriminative identity cues while capturing both cross-modal consistency and modality uniqueness. In addition, a modality relational knowledge distillation is proposed, which transfers intra-modality relationships from the modality-specific space and inter-modality relationships from the modality-shared space into the modality-contextual feature space through hierarchical cosine angular distillation, effectively reducing cross-modal discrepancies and intra-modal interference while enhancing the discriminability and robustness of feature representations.

\section{Conclusion}
This paper develops a novel MDCN method to address the challenges of VI-ReID. To the best of our knowledge, MDCN is the first method to introduce modality-contextual text prompts. It jointly leverages CPDL and SCL to facilitate effective multi-modal, multi-feature space learning. Specifically, CPDL uses prompts to decouple features and enforce modality orthogonality, ensuring that modality information is clearly distinguished from identity information. SCL embeds the learned high-level semantic information into visual representations, guiding the image encoder to emphasize critical identity-related attributes.  Furthermore, MRKD is proposed to integrate valuable high-level semantic relationships from both modality-shared and modality-specific feature spaces into the modality-contextual feature space, significantly enhancing the extraction of identity-aware valuable information. Extensive experiments on SYSU-MM01, RegDB, and LLCM datasets demonstrate that MDCN consistently achieves robust cross-modal feature alignment and surpasses existing methods under challenging visible-infrared settings. Visualization analyses further reveal that MDCN produces more compact and discriminative feature clusters, effectively emphasizing cross-modal semantic regions while reducing noise. Beyond accuracy gains, our method shows strong potential for real-world cross-spectral surveillance applications, where reliable identity matching under diverse lighting conditions is essential. The integration of CPDL, SCL, and MRKD not only enhances representation discriminability but also establishes a scalable pathway for broader multi-modal learning paradigms. 

\hl{With the rapid convergence of vision and language technologies, extending MDCN to incorporate textual cues or human‑in‑the‑loop feedback may further strengthen its applicability to intelligent monitoring, forensic analysis, and cross‑modal security analytics. For example, when a target is partially occluded, operators could interactively adjust the learned person‑related prompts, such as emphasizing a specific clothing color, to refine retrieval of ambiguous identities on the fly without retraining. Such interactive prompt tuning injects human prior knowledge into the frozen semantic anchors, making the system more adaptable to hard cases. We believe that jointly modeling visible, infrared, and auxiliary textual information, augmented with flexible human guidance, will pave the way for more accurate, interpretable, and trustworthy cross‑modal retrieval in practical deployments.}



\section{Conclusion}
This paper proposes a novel Modality Decoupling and Coupling Network (MDCN) to address the key challenges of visible-infrared person re-identification. To the best of our knowledge, MDCN is the first method to introduce modality-contextual text prompts for VI-ReID. By jointly integrating CPDL, SCL, and MRKD, MDCN enables effective multi-modal and multi-feature space learning. Specifically, CPDL decouples modality-contextual prompts into person-related and modality-related prompts and enforces modality orthogonality, allowing identity information and modality information to be clearly separated. Based on these decoupled semantic prompts, SCL embeds high-level semantic guidance into visual representations and encourages the image encoder to learn more stable modality-shared, modality-specific, and modality-contextual features. Furthermore, MRKD transfers valuable intra-modality and inter-modality semantic relationships from modality-specific and modality-shared feature spaces into the modality-contextual space, thereby enhancing identity-aware feature representation. Extensive experiments on SYSU-MM01, RegDB, and LLCM demonstrate that MDCN achieves strong and robust cross-modal retrieval performance under challenging visible-infrared settings. Visualization results further show that MDCN can produce more compact and discriminative feature clusters while focusing on meaningful cross-modal semantic regions and reducing noise. Beyond performance improvement, MDCN also shows strong potential for practical cross-spectral surveillance applications, where reliable identity matching under diverse lighting conditions is essential. In future work, MDCN could be further extended by incorporating auxiliary textual cues and human-in-the-loop feedback. For example, when a target is partially occluded or visually ambiguous, human operators could provide simple feedback, such as emphasizing visible clothing colors or reliable body regions, to guide prompt adaptation and refine retrieval results without retraining the whole model. Such interactive guidance may improve the adaptability, interpretability, and trustworthiness of MDCN in complex real-world deployment scenarios.