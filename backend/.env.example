# 复制本文件为 backend/.env 并填入真实值（.env 已被 gitignore，不会进仓库）。

OPENAI_API_KEY=
BASE_URL=https://api.openai.com/v1
MODEL_NAME=Qwen3.6-35B
EMBEDDING_MODEL=Qwen3-Embedding-8B
EMBEDDING_BASE_URL=https://api.openai.com/v1

# JWT 签名密钥（必填）：缺失或使用公开弱值时服务启动即失败。
# 生成：python -c "import secrets; print(secrets.token_urlsafe(48))"
JWT_SECRET=

# 注册邀请码：留空时完全关闭注册；填写后只有提交匹配邀请码的用户可以注册。
# 生成：python -c "import secrets; print(secrets.token_urlsafe(24))"
REGISTRATION_INVITE_CODE=

# 用户 LLM API Key 的数据库加密主密钥（必填且部署后必须稳定保管）。
# 生成：python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
LLM_SETTINGS_ENCRYPTION_KEY=

# 两个匿名公开聊天入口按来源 IP 共享的中国自然日请求额度。
PUBLIC_CHAT_DAILY_IP_LIMIT=10

# 同源 Docker 部署通常不需要 CORS。启用 HTTPS 后必须把 COOKIE_SECURE 改为 true。
COOKIE_SECURE=false
COOKIE_SAMESITE=lax
CORS_ALLOW_ORIGINS=
